begin transaction

	;WITH ToDelete AS
	(
	  SELECT
		id, numero, texto, dataRegistro, ROW_NUMBER() OVER (PARTITION BY numero, texto ORDER BY dataRegistro Desc /*???, Id???*/) AS rn
	  FROM
		tbTeste 
	)

	DELETE FROM ToDelete WHERE (rn > 1 AND dataRegistro IS NULL)

	SELECT id, numero, texto, dataRegistro FROM tbTeste

rollback transaction